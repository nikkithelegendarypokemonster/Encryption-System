    for(int i=s;i<n;i++){
        if(inside_queue(i)==0){
            if(proc[i].AT>=start && proc[i].AT<=end && i!=past){
                if(proc[i].name<current_proc && proc[i].AT >cur_at){
                    loc=i;
                    current_proc=proc[i].name;
                }
            }
        }
    }